# Cravio Admin — status

**No code here yet.** This package is scaffolded (this file + `CLAUDE.md`) but not implemented — that's expected at the current project stage (Phase 0, see root `CLAUDE.md`).

It will become the internal ops dashboard + brand web dashboard. Per `docs/P0-task-cards.md`, brand-facing pieces of this package start with card **D3** (discovery + shortlist UI) and **C2** (campaign builder UI) — both depend on API cards (D1/D2, C1) landing first. Nothing to set up here until one of those cards is picked up.

See `CLAUDE.md` in this folder for the intended stack and conventions. Once real code lands, this README will be replaced with actual install/run/test steps (matching the pattern in `/api/README.md` and `/mobile/README.md`).

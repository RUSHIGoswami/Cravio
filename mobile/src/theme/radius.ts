/**
 * Cravio — Radius scale (React Native)
 * Derived from tokens/radius.css.
 */

export const radius = {
  sm: 8,      // chips, small controls
  md: 12,     // buttons, inputs
  lg: 16,     // cards
  xl: 22,     // sheets, large surfaces
  pill: 999,  // status pills, filter pills, avatars
} as const;
